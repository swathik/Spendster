%div.wrapper
  .left-bar
    %h3 Welcome to Spendster
    %p
      Spendster is a way to manage your spends.
      Create a budget list, set limits for each
      category. And start adding spends
      
  .right-bar
    %div{ class: 'row' }
      %div{ class: 'span5 offset2' }
    
        %h1 Sign up
        = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { class: 'well'}) do |f|      
          - if resource.errors.messages
            %ul.devise_errors
              - resource.errors.messages.each do |key, message|
                %li= "#{key.capitalize} #{message.first}"
          %div
            = f.label :username
            = f.text_field :username
          %div
            = f.label :email
            = f.email_field :email
          %div
            = f.label :password
            = f.password_field :password
          %div
            = f.label :password_confirmation
            = f.password_field :password_confirmation
          %div
            = f.submit "Sign up", class: 'btn btn-info'
        = render "devise/shared/links"